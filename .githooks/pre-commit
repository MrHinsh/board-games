#!/bin/sh
# Runs the repo smoke check before every commit.
# Enable once per clone: git config core.hooksPath .githooks
pwsh -NoProfile -File "$(git rev-parse --show-toplevel)/scripts/Test-Repo.ps1" || {
    echo "pre-commit: Test-Repo.ps1 failed - commit aborted" >&2
    exit 1
}
