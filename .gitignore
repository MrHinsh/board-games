# Local secrets/cache — never commit credentials
.local/

# Build artifacts
tools/bgg-mcp/build/
tools/bgg-mcp/bgg-mcp.exe

# Canonical-data checkpoints (local safety copies, rebuilt constantly)
data/state/checkpoints/

# Rebuildable conference source and BGG detail caches
data/working/conferences/
